package utilities;

public class Formatting {

    public static String concat_text(String prox_estado, char gravar_caracter, String direcao) {
        return ("(" + prox_estado + "," + gravar_caracter + "," + direcao + ")");
    }
}
